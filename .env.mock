# Mock 模式（vite --mode mock）
# mock 模式下请求由本地 mock 实现拦截，不会真正命中该地址；
# 保留一个合法值仅为避免模块初始化时 serverAddr 为空。
VITE_API_SERVER_ADDR=test.api.fudan.wisepen.oriole.cn

# Service 注册表（相对项目根目录）；mock 使用 mock 注册表
SERVICES_REGISTRY=./src/contexts/ServicesContext/registry.mock.ts
